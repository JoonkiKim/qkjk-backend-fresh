과제 19 해결 방법

fetchLoginUser만들때는  context.req.user.id에서 id를 뽑아오고, 그 id로 유저정보를 조회하는 서비스를 하나 만들어서 그 리턴값이 유저정보가 되도록 만들어서 조회했다


updateUserPwd함수를 user리졸버에 만든다 -> 비밀번호 검증 로직은 8자리 이상이 되도록 한다
전체 수정하도록 하고 액세스토큰 가드 & 아이디 일치여부 확인으로 검증
- 토큰 없을때 잘 막히는지
- 8자리 검증 되는지
- 아이디 다르면 잘 막히는지 -> 토큰에 담긴 아이디와 입력하는 아이디가 다르면 에러 던진다
- 이때 새로 만드는 비밀번호도 해시처리해야됨!
- 비밀번호가 잘 바뀐다

deleteLoginUser
softRemove를 하자
- 토큰이 없으면 실패
- 유저 아이디가 없으면 실패
- 토큰도 있고 유저아이디도 있으면 성공
- 삭제 후에는 로그인, fetch모두 불가능하다
